
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user profile, who is also a designer.",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth User ID." },
        "name": { "type": "string", "description": "User's display name." },
        "email": { "type": "string", "description": "User's email address.", "format": "email" },
        "photoURL": { "type": "string", "description": "URL of the user's profile picture.", "format": "uri" },
        "coverPhotoURL": { "type": "string", "description": "URL of the user's cover photo.", "format": "uri" },
        "specialization": { "type": "string", "description": "The designer's main specialization." },
        "bio": { "type": "string", "description": "A short biography of the designer." },
        "subscriberCount": { "type": "number", "description": "Number of users following this designer." },
        "followers": { "type": "array", "items": { "type": "string" }, "description": "List of UIDs of users who follow this designer." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of user creation." }
      },
      "required": ["uid", "name", "email", "createdAt", "subscriberCount", "followers"]
    },
    "Project": {
      "title": "Project",
      "type": "object",
      "description": "Represents a design portfolio project.",
      "properties": {
        "name": { "type": "string", "description": "Name of the portfolio project." },
        "designerId": { "type": "string", "description": "UID of the user who created the project." },
        "imageUrl": { "type": "string", "description": "URL of the project's main image.", "format": "uri" },
        "imageUrls": { "type": "array", "items": { "type": "string", "format": "uri" }, "description": "Array of URLs for the project's images." },
        "description": { "type": "string", "description": "Detailed description of the project." },
        "tags": { "type": "array", "items": { "type": "string" }, "description": "Descriptive tags for the project." },
        "tools": { "type": "array", "items": { "type": "string" }, "description": "Tools used to create the project." },
        "viewCount": { "type": "number", "description": "Number of times the project has been viewed." },
        "likeCount": { "type": "number", "description": "Number of likes for the project." },
        "likes": { "type": "array", "items": { "type": "string" }, "description": "List of UIDs of users who liked this project." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of project creation." }
      },
      "required": ["name", "designerId", "imageUrl", "imageUrls", "description", "createdAt", "viewCount", "likeCount", "likes"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores all user profiles. The document ID is the user's Firebase Auth UID."
    },
    "/projects/{projectId}": {
      "schema": { "$ref": "#/entities/Project" },
      "description": "Stores all portfolio projects created by users."
    }
  }
}

